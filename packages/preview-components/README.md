# ShipiOS Preview Components

This package will hold React components used to render browser previews from ShipiOS schema data.

Rules:

- components must stay deterministic
- preview behavior should mirror export assumptions
- avoid product-page UI concerns here

