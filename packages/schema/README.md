# ShipiOS TypeScript Schema Package

This package should expose the TypeScript representation of the ShipiOS app schema.

Long-term direction:

- generate types from the canonical Rust or JSON schema contract
- avoid hand-maintained drift between TypeScript and Rust

