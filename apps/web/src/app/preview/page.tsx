import { PreviewClient } from "@/components/app-launch-kit/preview-client";

export default function PreviewPage() {
  return (
    <main className="mx-auto flex w-full max-w-6xl flex-1 flex-col px-6 py-8 sm:px-10 lg:px-12">
      <PreviewClient />
    </main>
  );
}

